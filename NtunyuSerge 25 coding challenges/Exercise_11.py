# Calculate the average of the numbers in an array of numbers
def average_of_numbers(array):
    return sum(array)/len(array)


my_array = [12, 3, 15, 0, 0, 0]
print(average_of_numbers(my_array))