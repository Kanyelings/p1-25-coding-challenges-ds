def mult_table(number, maxlimit):
    for values in range(maxlimit):
        answer = values*number
        print(f"{values} x {number} = {answer}")

mult_table(7, 13)

#getting two arguments to find the multiplication table of a number with
#the first agument being the number and the second argument being the
#how long th multiplication table should be


